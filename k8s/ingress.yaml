apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: churn-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: churn-api-service
            port:
              number: 80